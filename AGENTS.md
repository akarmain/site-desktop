# AGENTS.md

Инструкции для AI-агентов и контрибьюторов — в **[docs/ai/README.md](docs/ai/README.md)**.

Кратко: контент правится в `app/data/*` и `i18n/locales/*` (не в шаблонах); стили — только
CSS-переменные `--ak-*` (Tailwind/shadcn убраны); меньше кода — лучше.
