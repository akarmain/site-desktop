# AGENTS.md

Инструкции для AI-агентов и контрибьюторов — в **[docs/ai/README.md](docs/ai/README.md)**.

Структура репозитория: `frontend/` (Nuxt), `backend/` (FastAPI), `docs/`.

Кратко: контент правится в `frontend/app/data/*` и `frontend/i18n/locales/*` (не в шаблонах); стили — только
CSS-переменные `--ak-*` (Tailwind/shadcn убраны); меньше кода — лучше.
